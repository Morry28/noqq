0.1.2: Multi file ( table ) support
-----

Change:
(noqq)filePath - z string na array of string
(noqq) sample - z string na any of any[]
(noqq) initFile() - array support v this.sample -type of string? sampleFile():sampleFileArray()

(filemanager) filepath - -||-
(filemanager) filetype - z priameho volania otazka ? string : string[]

Add:
(filemanager) getFileType a check ci su vsetky csv

Redundancia: (turingmachinefactory) newWork()
        const fileManager = new FileManager(filePath)
        this.workFileType = fileManager.fileType
moze File manager ma byt injected as dependency

Add:
(turingmachinefactory) ak obsahuje Filemanager array spusti (TODO)


